// import Binance from 'binance-api-node'
// const client = Binance()

export const getCandles = ({ symbol, interval, number }) => {
  console.log(`Get ${number} candles for ${symbol} with interval: ${interval}`)
}
